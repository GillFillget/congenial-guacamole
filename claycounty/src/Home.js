import map from './Photos/mapcc.png'

const Home = () => {
    return ( 
        <div className="map">
            <img src={ map } alt="Map of Clay County, Kansas" className='map'/>
        </div>
     );
}
 
export default Home
